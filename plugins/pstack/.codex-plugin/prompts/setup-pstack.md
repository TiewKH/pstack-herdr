---
name: setup-pstack
description: configure pstack per-role model choices and an optional Herdr reasoning-budget preset
disable-model-invocation: true
---

Invoke the `setup-pstack` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `poteto-mode/references/codex-tools.md`, including its Per-skill notes.
